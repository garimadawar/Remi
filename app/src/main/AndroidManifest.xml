<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gd.remi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/industry"
        android:label="@string/app_name"
        android:roundIcon="@drawable/industry"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".EmpActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegClientActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AllClientsActivity" />
        <activity android:name=".AllEmpsActivity" />
        <activity android:name=".AddProductActivity"></activity>
    </application>

</manifest>